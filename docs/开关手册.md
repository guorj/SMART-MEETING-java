# 智能会议系统 - 开关手册

文档版本：2026-06-07  
适用范围：`meeting-server`（`application.yml` + profile 覆盖 + 环境变量覆盖）

---

## 1. 目标与原则

本手册用于统一说明系统中可配置开关（feature flags / runtime toggles）：

- 已实现：可直接通过 yml / 环境变量生效。
- 建议新增：尚未落地，仅做运维与研发规划，不代表当前版本可用。

开关设计原则：

- 同一能力尽量“主开关 + 细分开关”。
- 对外部依赖（飞书、LLM、ASR）优先提供降级开关。
- 对定时任务优先提供 `enabled` 开关，避免误触发。

---

## 2. 配置生效优先级

Spring Boot 常规优先级（高 -> 低）：

1. 环境变量（如 `MEETING_TODO_REMINDER_ENABLED`）
2. profile 配置（如 `application-dev.yml`、`application-prod.yml`）
3. 默认配置 `application.yml`

说明：

- 大部分开关改动后需要重启服务生效。
- 使用 `@ConditionalOnProperty` 的组件（如定时任务）必须重启后才会注册/反注册。

---

## 3. 已实现开关（当前可用）

## 3.1 会后链路

| 配置键 | 默认值 | 作用 | 影响范围 |
|---|---|---|---|
| `meeting.minute.generation-enabled` | `false` | 会后是否生成纪要主链路 | 关闭后不触发 `MeetingEndedEvent`；与离线转写解耦，不影响 `offline-enabled` |
| `meeting.asr.offline-enabled` | `true` | 会后是否执行讯飞 IST 离线转写 | 会议结束时**独立**触发（Outbox `OFFLINE_ASR`）；有实时定稿分段时跳过；若同时开纪要则**先离线、再纪要** |
| `meeting.minute.llm-enabled` | `true` | 是否调用 LLM 生成纪要初稿 | 关闭后用转写原文 + 简易头作为纪要正文 |
| `meeting.minute.ai-enhancement-enabled` | `true` | 纪要 AI 增强开关 | 关闭后仅保留初稿 |
| `meeting.minute.persist-enabled` | `true` | 是否落库纪要正文 | 关闭后仅保留外链/状态 |
| `meeting.minute.feishu-doc-enabled` | `true` | 是否写入飞书文档 | 关闭后跳过 `createDoc`/`updateDoc`，不写入 `doc_url` |
| `meeting.minute.notify-enabled` | `true` | 纪要完成后是否推送飞书通知 | 关闭后跳过 `pushFeishuCard` |
| `meeting.minute.expose-content-in-api` | `true` | 纪要 API 是否返回全文 | 关闭后 API 仅返回元信息 |
| `meeting.todo.extraction-enabled` | `false` | 是否执行待办提取链路 | 关闭后不发布/不执行待办提取 |
| `meeting.todo.reminder-enabled` | `false` | 待办提醒定时任务总开关 | 关闭后不注册 `TodoReminderScheduler`（含逾期标记/升级/每日汇总三项子任务） |
| `meeting.todo.reminder-cron` | `0 */30 * * * ?` | 逾期自动标记 + 责任人提醒 Cron | 每 30 分钟扫描过 deadline 仍未完成的待办，自动置为 OVERDUE 并提醒责任人 |
| `meeting.todo.escalation-cron` | `0 30 8,17 * * ?` | 延期升级提醒直属上级 Cron | 每日 08:30 与 17:00 两次，按责任人聚合 OVERDUE 待办，向上级发送交互式卡片（含「催办」「已知晓暂不处理」按钮） |
| `meeting.todo.daily-summary-cron` | `0 0 9 * * ?` | 每日未完成待办清单汇总推送 Cron | 每日 09:00 按责任人聚合未完成待办，推送交互式卡片（含每条待办的「申请延期」「挂起」按钮，点击后引导回复理由） |
| `meeting.todo.reason-input-ttl-minutes` | `5` | 申请延期/挂起后理由输入会话 TTL（分钟） | 责任人点击按钮后须在此时长内回复理由文本，超时失效 |
| `meeting.todo.attachment-dir` | `data/todo-attachments` | 待办附件本地目录（冷启动） | 修改后重启 |
| `meeting.todo.attachment-max-bytes` | `10485760` | 单附件最大字节（冷启动） | 默认 10MB |

补充：

- `meeting.todo.reminder-*` / `meeting.todo.escalation-*` / `meeting.todo.daily-summary-*` 三组 Cron 均仅在 `reminder-enabled=true` 时生效（共用 `TodoReminderScheduler` Bean）。
- 延期升级提醒依赖 `int_user_mapping_feishu.supervisor_feishu_user_id`（v0.23 新增列）；未配置上级的用户跳过升级。
- 待办拆分（`POST /api/v1/todos/{tid}/split`）支持将父待办拆分为子待办，子待办 `parent_id` 指向父待办（v0.23 新增列）。
- 每日汇总卡片（`buildDailySummaryCard`）与上级升级卡片（`buildSupervisorEscalationCard`）均为交互式卡片：责任人点「申请延期/挂起」后机器人引导其回复理由（由 `TodoReasonPendingStore` 跟踪会话，5 分钟内有效）；上级点「催办」会向责任人再次提醒并递增 `remind_count`，「已知晓暂不处理」仅记录日志不改变待办状态。

**会后开关组合（2026-06-07 解耦后）**

| offline-enabled | generation-enabled | 会后行为 |
|---|---|---|
| true | false | `PROCESSING` → 离线转写入库 → `COMPLETED`（dev 常用） |
| false | true | 直接纪要（读 DB 实时分段，无则空转写） |
| true | true | `PROCESSING` → 离线转写完成 → 纪要生成 → `COMPLETED` |
| false | false | 直接 `COMPLETED` |

启用 Kafka 时需创建 topic `meeting.offline.asr`（dev 默认 LocalEventBus 无需 Kafka）。

## 3.2 调度与编排

| 配置键 | 默认值 | 作用 |
|---|---|---|
| `meeting.scheduler.pre-enabled` | `true` | 会前 PRE 自动触发总开关 |
| `meeting.scheduler.pre-window-minutes` | `5` | PRE 触发容忍窗口 |
| `meeting.scheduler.pre-24h-template-code` | 空 | 会前 24h 模板 |
| `meeting.scheduler.pre-10m-template-code` | 空 | 会前 10min 模板 |
| `meeting.scheduler.scan-ms` | `300000` | PRE 扫描周期（毫秒） |
| `meeting.pipeline.post-auto-trigger.enabled` | `true` | 会后 POST 自动触发开关 |
| `meeting.pipeline.post-auto-trigger.template-code` | 空 | POST 指定模板编码 |
| `meeting.cache.refresh-enabled` | `true` | 预设缓存定时刷新开关 |
| `meeting.cache.refresh-cron` | `0 */10 * * * ?` | 预设缓存刷新 Cron |

## 3.3 会序本地上传资料

| 配置键 | 默认值 | 作用 |
|---|---|---|
| `meeting.agenda-material.storage-dir` | `./data/agenda-materials` | 本地上传资料落盘目录（admin-server 与 meeting-server 须一致） |
| `meeting.agenda-material.max-image-bytes` | `10485760` | 单张图片最大字节（10MB） |
| `meeting.agenda-material.max-doc-bytes` | `20971520` | 单个 doc/docx 最大字节（20MB） |

首期白名单：jpg/png/gif/webp、doc/docx。主持页通过 `GET /api/v1/meetings/{id}/agenda-materials/{fileId}` 下载（JWT / `?token=`）。

## 3.4 中台能力与外部依赖

| 配置键 | 默认值 | 作用 |
|---|---|---|
| `meeting.kafka.enabled` | `false` | Kafka / LocalEventBus 双形态切换 |
| `meeting.audio.cloud-download-enabled` | `true` | 会后是否允许云端录音下载兜底 |
| `meeting.asr.realtime-enabled` | `false` | 会中实时转写开关（**保留能力，默认关**） |
| `meeting.vc.recording-enabled` | `false` | 飞书 VC 云端录制开关（远程参会人音频通过飞书录制回调获取，见 [feishu-vc-recording-design.md](feishu-vc-recording-design.md)） |
| `meeting.asr.offline-role-enabled` | `true` | 离线 IST 上传是否开启说话人分离（false 时 roleType=0） |
| `meeting.asr.offline-role-mode` | `auto` | IST 角色分离：`auto`（≥2 声纹→roleType=3，否则 1）、`blind`、`voiceprint` |
| `meeting.asr.offline-role-num-hint-enabled` | `true` | 是否向 IST 传 roleNum（参会人数 hint，0–10） |
| `meeting.voiceprint.offline-label-enabled` | `true` | 会后离线转写是否做 ISV 声纹 1:N 并回写姓名 |
| `meeting.voiceprint.offline-min-slice-ms` | `3000` | 声纹代表切片最小时长（毫秒） |
| `meeting.voiceprint.offline-max-speakers` | `8` | 单场最多 ISV 处理的 distinct IST 簇数 |
| `meeting.voiceprint.offline-vote-slices` | `3` | 簇级 ISV 投票：每簇取最长句段数 |
| `meeting.voiceprint.offline-segment-relabel-enabled` | `true` | 未命名簇是否按句段逐段 ISV 补标（balanced） |
| `meeting.voiceprint.offline-split-cluster-enabled` | `true` | 同 IST 簇内多 feature 是否允许分裂 |
| `meeting.isv.match-score-threshold` | `0.6` | ISV 1:N 最低置信度（全库 topK 结果中取最高分过线） |
| `meeting.isv.search-top-k-max` | `10` | ISV 1:N 请求 topK（1–10，讯飞 API 上限；与参会人数无关） |
| `meeting.isv.search-top-k-min` | `3` | topK 钳制下限（`effectiveTopK` 用） |
| `meeting.host.enabled` | `true` | AI 主持总开关 |
| `meeting.host.agenda-enabled` | `true` | 主持会序推进能力 |
| `meeting.host.tts-enabled` | `true` | 主持 TTS 开关 |
| `meeting.host.roll-call-enabled` | `true` | 混合检点开关 |
| `meeting.host.auto-roll-call-after-opening` | `true` | 开场后自动检点开关 |
| `meeting.notification.bot-enabled` | `false` | 会后通知走 scheduled-bot 网关 |
| `meeting.asr.max-concurrent-sessions` | `3` | 会中实时 ASR WebSocket 并发路数（按 meetingId 连接池） |
| `meeting.async.task-executor-core` | `16` | 通用异步线程池 core |
| `meeting.async.task-executor-max` | `32` | 通用异步线程池 max |
| `meeting.async.task-executor-queue` | `200` | 通用异步线程池队列 |
| `openclaw.max-concurrent-invokes` | `5` | OpenClaw MCP 并行 invoke 上限（替代全局 Semaphore(1)） |
| `spring.datasource.hikari.maximum-pool-size` | `20`（prod 建议 `40`） | JDBC 连接池上限 |

---

## 4. 已有定时任务与开关现状

| 任务类 | 触发配置 | 当前是否有 `enabled` |
|---|---|---|
| `TodoReminderScheduler` | `reminder-cron` + `escalation-cron` + `daily-summary-cron` | 是（`meeting.todo.reminder-enabled`） |
| `PresetCacheRefreshScheduler` | `meeting.cache.refresh-cron` | 是（`meeting.cache.refresh-enabled`） |
| `MeetingScheduler` | `meeting.scheduler.scan-ms` | 部分（逻辑内 `pre-enabled`） |
| `RecordingSilenceAutoPauseScheduler` | 固定 60s | 否（仅通过 `silence-auto-pause-min<=0` 近似关闭） |
| `RecordingTimeoutChecker` | 固定 5 分钟 + 每小时 | 否 |
| `AudioCacheCleaner` | 固定凌晨 3 点 + 周报 | 否 |
| `OutboxPoller` | `meeting.outbox.poll-delay-ms` | 否 |
| `PipelineTimeoutScanner` | `meeting.pipeline.timeout-scan-ms` | 否 |

---

## 5. 建议新增开关（暂未实现）

以下为建议项，当前版本不可直接配置生效：

## 5.1 P0（优先建议）

| 建议配置键 | 目标 |
|---|---|
| `meeting.recording.timeout-check-enabled` | 控制 `RecordingTimeoutChecker` |
| `meeting.audio.cache-clean-enabled` | 控制 `AudioCacheCleaner` |
| `meeting.outbox.poll-enabled` | 控制 `OutboxPoller` |
| `meeting.pipeline.timeout-scan-enabled` | 控制 `PipelineTimeoutScanner` |

## 5.2 P1（运维友好）

| 建议配置键 | 目标 |
|---|---|
| `meeting.feishu.send-enabled` | 飞书发送总闸（用户/群/文档写入） |
| `meeting.todo.sync-feishu-enabled` | 控制待办同步到飞书 |
| `meeting.todo.reminder-skip-invalid-assignee-enabled` | 跳过 `vp_` / `unknown` 责任人 |

## 5.3 P2（统一治理）

| 建议配置键 | 目标 |
|---|---|
| `meeting.scheduler.enabled` | 全局调度总闸（子任务为二级开关） |
| `meeting.audio.silence-auto-pause-enabled` | 显式控制静音自动暂停 |

---

## 6. 推荐运维组合

## 6.1 “只保留会中录音，不做会后”场景

```yaml
meeting:
  minute:
    generation-enabled: false
  todo:
    extraction-enabled: false
    reminder-enabled: false
```

## 6.2 生产推荐（默认：仅录音 + 会后离线声纹）

```yaml
meeting:
  asr:
    realtime-enabled: false
    vc-recording-enabled: false
    offline-enabled: true
    offline-role-enabled: true
    offline-role-mode: auto
    offline-role-num-hint-enabled: true
  voiceprint:
    offline-label-enabled: true
    offline-vote-slices: 3
    offline-segment-relabel-enabled: true
    offline-split-cluster-enabled: true
  isv:
    enabled: true
    match-score-threshold: 0.55
  minute:
    generation-enabled: true
    llm-enabled: true
    ai-enhancement-enabled: true
    persist-enabled: true
    feishu-doc-enabled: true
    notify-enabled: true
```

会前建议完成组织级声纹注册（工作台入口）；会后纪要正文将带「姓名: 发言」格式（识别失败时保留 `speaker_N`）。

**双场景说明（确定参会人 / 临时会 / 会中临时加人）**：

- IST：有 ≥2 条 featureId（participant 行或联查声纹库）时 `auto` 走声纹辅助分离；否则盲分。
- ISV：**全库** 1:N，不按参会人表过滤；临时列席、嘉宾只要在 `int_voiceprint` 已注册即可标名。
- `search-top-k-max` 建议大库拉满 `10`；误标风险靠 `match-score-threshold`、簇级投票与按租户拆 ISV `groupId` 控制。

## 6.3 会中实时字幕场景

```yaml
meeting:
  asr:
    realtime-enabled: true
```

需讯飞实时路数；纪要选择性走 `int_transcript_segment` 实时分段，**不**覆盖为离线转写。主持/录音主页底部 UI 将切换为**实时字幕区**（`asr_started`）；关闭时显示离线录音状态条。

## 6.4 “保留纪要，不要待办提醒”场景

```yaml
meeting:
  minute:
    generation-enabled: true
  todo:
    extraction-enabled: true
    reminder-enabled: false
```

## 6.5 生产安全降载（建议）

- 明确设置每个关键开关，不依赖默认值。
- 所有 `enabled` 配置在 `application-prod.yml` 显式声明并留注释。
- 发布后核对一次启动日志，确认关键 Scheduler 注册状态与预期一致。

## 6.6 纪要分步调试组合

| 场景 | 关键配置 |
|---|---|
| 只要 PCM + 离线转写入库，不要纪要 | `generation-enabled=false`（PCM 仍会写 `data/audio`） |
| 要纪要但不要 LLM / AI 增强 | `generation-enabled=true`，`llm-enabled=false`，`ai-enhancement-enabled=false` |
| 要 LLM 初稿，不要 AI 增强 | `llm-enabled=true`，`ai-enhancement-enabled=false` |
| 只要库内纪要，不要飞书 | `feishu-doc-enabled=false`，`notify-enabled=false` |
| 不要离线 ASR（仅有实时分段时用） | `offline-enabled=false`，`realtime-enabled=true` |
| 远程参会人音频采集 | `vc-recording-enabled=true`，配合飞书 VC 云端录制回调 | 纯线上会或混合会（主持人入飞书 + 共享麦克风）可覆盖全员；详见 [feishu-vc-recording-design.md](feishu-vc-recording-design.md) |

示例：只要库内纪要、不要飞书与 LLM

```yaml
meeting:
  minute:
    generation-enabled: true
    llm-enabled: false
    ai-enhancement-enabled: false
    persist-enabled: true
    feishu-doc-enabled: false
    notify-enabled: false
```

---

## 7. 变更流程建议

1. 先更新本手册（记录新增/废弃开关）。  
2. 再做代码实现。  
3. 同步更新 `application.yml` / `application-dev.yml` / `application-prod.yml` 示例值。  
4. **取值范围**：Admin 热更键的 min/max 与出厂默认见 [config-ranges.md](./config-ranges.md)；系统参数页保存时会校验范围。

---

## 8. Admin 系统参数与取值范围（2026-06-08）

**三层配置**

| Admin 页标识 | 含义 | 存储 |
|--------------|------|------|
| **可热加载**（绿标） | 保存至 `int_meeting_system_config`，点「通知 meeting-server 热加载」 | DB |
| **冷启动**（灰标） | 只读 mirror 当前 YAML；改 `application.yml` / env 后须重启 | YAML |
| （不在本页） | 部署/密钥：库连接、讯飞/飞书/LLM 鉴权等 | YAML |

- **可热加载键**：写入 `int_meeting_system_config`；schema 返回 `minValue`/`maxValue`/`valueRangeHint`；系统参数页工具栏可筛选「可热加载 / 冷启动」。
- **冷启动键**：完整 16 项清单见 [config-ranges.md](./config-ranges.md) §冷启动；含 `meeting.kafka.enabled`、`meeting.audio.*`、`openclaw.agent.provider` 等。
- **YAML 注释**：`application.yml` 文件头 `[Admin热更]` / `[冷启动]` / `[部署/密钥]` 标签与 Admin 徽章对应。
- **业务开关禁止写回 profile YAML**：`meeting.pipeline.*`、`meeting.scheduler.pre-*`（除 `scan-ms`）、`meeting.isv.enabled` / `match-score-threshold` 等 **Admin 热更** 键在 `MeetingRuntimeConfigLoader.reload()` 后由 Java 出厂 + DB 覆盖；dev profile 中历史块已列为失效项，见 [config-ranges.md §YAML 必要性审计](./config-ranges.md#yaml-必要性审计meeting-server)。调业务开关请用 **seed + Admin 系统参数**，勿改 `application-dev.yml`。
- 全量范围索引：[config-ranges.md](./config-ranges.md)
