# Java 版开发校验修复计划

> 日期：2026-05-09 | 基于 DEVELOPMENT.md v0.2 校验报告
> 原则：最小改动范围，补齐缺失不碰已完成的模块

---

## 修复任务总览

| 编号 | 类别 | 任务 | 影响范围 | 优先级 |
|------|------|------|----------|--------|
| F1 | DTO | 新增 `PreviousProgressResponse.java` | 1个新文件 | 🔴 |
| F2 | DTO | 新增 `StartMeetingResponse.java` | 1个新文件 | 🔴 |
| F3 | Entity | 新增 `UserMapping.java` | 1个新文件 | 🟡 |
| F4 | API | MeetingController 补齐 `endMeeting` + `getPreviousProgress` | 1个文件修改 | 🔴 |
| F5 | Service | MeetingService 补齐 `endMeeting()` 方法 | 1个文件修改 | 🔴 |
| F6 | Service | TodoExtractionService 从骨架→完整实现 | 1个文件重写 | 🔴 |
| F7 | ASR | XfyunOfflineClient 从空壳→完整实现 | 1个文件重写 | 🔴 |
| F8 | ASR | XfyunIsvClient 从空壳→完整实现 | 1个文件重写 | 🟡 |
| F9 | Service | VoiceprintService 从骨架→完整实现 | 1个文件重写 | 🟡 |
| F10 | Scheduled | RecordingTimeoutChecker 补充逻辑 | 1个文件修改 | 🟢 |
| F11 | Scheduled | AudioCacheCleaner 补充逻辑 | 1个文件修改 | 🟢 |

---

## F1 — PreviousProgressResponse.java（新增）

**依据**：DEVELOPMENT.md §6.3.1 已定义完整结构
**内容**：
- `lastMeetingId`, `lastMeetingTitle`, `lastMeetingTime`
- `totalCount`, `completedCount`, `inProgressCount`, `delayedCount`
- 内部类 `DelayedItem`（content, assigneeName, blockReason, deadline）
- Lombok `@Data @Builder`

**位置**：`meeting-server/src/main/java/com/smartmeeting/api/dto/PreviousProgressResponse.java`

---

## F2 — StartMeetingResponse.java（新增）

**依据**：DEVELOPMENT.md §6.3.1 已定义结构
**内容**：
- `meetingId`, `newStatus`, `previousProgress`（可为null）
- Lombok `@Data @Builder`

**位置**：`meeting-server/src/main/java/com/smartmeeting/api/dto/StartMeetingResponse.java`

---

## F3 — UserMapping.java（新增）

**依据**：DEVELOPMENT.md §6.1 实体类定义 + DDL §2.7
**内容**：
- `userId` (主键), `userName`, `feishuUserId`, `feishuUnionId`, `feishuOpenId`
- MyBatis-Plus `@TableName("int_user_mapping")`

**位置**：`meeting-server/src/main/java/com/smartmeeting/entity/UserMapping.java`

---

## F4 — MeetingController 补齐（修改）

**新增接口**：

```java
// 结束会议（F-MID-END）
@PostMapping("/{id}/end")
public ApiResponse<MeetingResponse> endMeeting(@PathVariable String id)

// 上次会议进度查询（F-MID-02）
@GetMapping("/{id}/previous-progress")
public ApiResponse<PreviousProgressResponse> getPreviousProgress(@PathVariable String id)
```

**改动**：在现有 `MeetingController.java` 末尾新增 2 个方法，注入 `MeetingMapper`（查询上次进度用）

---

## F5 — MeetingService.endMeeting()（修改）

**新增方法**：
```java
@Transactional
public MeetingResponse endMeeting(String meetingId) {
    // 1. 查询会议
    // 2. 状态校验（仅 RECORDING/PAUSED 可结束）
    // 3. 更新状态 → PROCESSING
    // 4. 记录 actual_end_time + duration_seconds
    // 5. 发送 Kafka 消息触发纪要生成链（或 LocalEventBus 降级）
    // 6. 返回 MeetingResponse
}
```

**改动**：在现有 `MeetingService.java` 末尾新增方法，注入 `KafkaProducer` + `LocalEventBus`

---

## F6 — TodoExtractionService（重写）

**依据**：DEVELOPMENT.md §6.7 TodoExtractionService 逻辑描述
**从 13 行骨架 → 预计 ~150 行完整实现**

**核心逻辑**：
1. 接收纪要全文 + 参会人列表
2. 调用 LLM 提取待办项（JSON格式）：`[{content, assigneeName, priority, deadline}]`
3. 按 assigneeName → userId 在 int_meeting_participant 中匹配责任人
4. 写入 int_meeting_todo（status=PENDING）
5. 更新 int_meeting_participant.todo_count
6. 触发飞书待办通知卡片

---

## F7 — XfyunOfflineClient（重写）

**依据**：参照 Python 版 `asr/offline_client.py`（283行）+ DEVELOPMENT.md §10.2
**从 14 行空壳 → 预计 ~250 行完整实现**

**核心逻辑**：
- 使用讯飞离线语音转写 HTTP API
- 上传音频文件 → 轮询任务状态 → 获取转写结果
- 返回 `List<TranscriptSegment>` 带时间戳分段
- 断点续传 + 指数退避重试

---

## F8 — XfyunIsvClient（重写）

**依据**：参照 Python 版 `feishu/voiceprint_isv.py` + DEVELOPMENT.md §10.3
**从 19 行空壳 → 预计 ~200 行完整实现**

**核心逻辑**：
- 讯飞 ISV 声纹 API 封装
- `registerVoiceprint(audio, userId)` → 注册声纹
- `identifySpeaker(audio)` → 声纹识别
- `deleteVoiceprint(featureId)` → 删除声纹

---

## F9 — VoiceprintService（重写）

**依据**：DEVELOPMENT.md §6.8
**从 16 行骨架 → 预计 ~120 行完整实现**

**核心逻辑**：
- 封装 XfyunIsvClient 调用
- 声纹注册：从录音片段提取 → 调用 ISV API → 写 int_voiceprint
- 声纹识别：将转录段音频 → 调用 ISV API → 更新 TranscriptSegment.speakerName
- 缓存声纹特征 ID（Redis）

---

## F10 — RecordingTimeoutChecker（补充）

**修改已有 15 行 → 预计 ~40 行**
- `@Scheduled(fixedRate = 60000)` 每分钟检查
- 查询状态 RECORDING 且 actual_start_time 超过 4 小时的会议
- 自动调用 endMeeting 触发超时结束

---

## F11 — AudioCacheCleaner（补充）

**修改已有 21 行 → 预计 ~50 行**
- `@Scheduled(cron = "0 0 3 * * ?")` 每天凌晨3点
- 清理 `/data/audio/` 下超过 168 小时（7天）的 PCM 文件
- 仅清理 COMPLETED/ABORTED 状态会议的音频

---

## 改动总结

| 统计项 | 数量 |
|--------|------|
| 新增文件 | 3（2 DTO + 1 Entity）|
| 修改文件 | 2（MeetingController + MeetingService）|
| 重写文件 | 4（TodoExtractionService + XfyunOfflineClient + XfyunIsvClient + VoiceprintService）|
| 补充文件 | 2（RecordingTimeoutChecker + AudioCacheCleaner）|
| **总计** | **11 个文件** |
| 预计新增代码 | ~1200 行 |

---

## 执行顺序

```
F1 → F2 → F3 → F4 → F5 → F6 → F7 → F8 → F9 → F10 → F11
 └─增DTO─┘ └─增Entity─┘ └─改Controller─┘ └───────重写Service───────┘ └─补Scheduled─┘
                           └─改Service──┘     └───────重写ASR─────────┘
```

---

*等待审阅确认后开始执行。*
