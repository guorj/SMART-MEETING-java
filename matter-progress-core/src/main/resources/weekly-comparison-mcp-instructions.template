## 会前事项对比 — 执行手册（oabp SOURCE + items JSON 入库）

> **v0.26+**：SOURCE 已改为 preset `host_agenda.oabpTaskSql`（oabp 库只读 SELECT）。Bot **只下发已填写的 SQL**；某 config 未填 `oabpTaskSql` 则**跳过**，**不导致任务失败**。**禁止**再读飞书多维表/docx 作为 SOURCE；**禁止**创建飞书通报 Doc；**禁止**输出 `generatedReportUrl=`。

### 0. 使用哪个 Skill、哪些 MCP（必须遵守）

| 类型 | 名称 | 用途 |
|------|------|------|
| **Skill** | `matter-progress` | 本任务专用（Bot 以 `/skill:matter-progress` 触发） |
| **MCP 读 SOURCE（oabp）** | `meeting-mysql__mysql_query` | 执行 [source_oabp_sql] 中每条 SELECT（只读，原样） |
| **MCP 读纪要** | `meeting-mysql__mysql_query` | 执行 [minute_query_sql]（库 `intelligence`） |
| **MCP 读上期通报（可选）** | `meeting-mysql__mysql_query` | 执行 [output_reference_sql]（库 `intelligence`，若有） |

- oabp 默认 schema：**{{oabpSchema}}**（MCP 连接默认库非 oabp 时，SQL 内表名须带 schema 前缀，如 `{{oabpSchema}}.jq_todos_task`）
- **禁止**调用 lark-mcp 读 SOURCE / 写 Doc
- **禁止**臆造数据；SQL 执行失败时在最终回复简述原因，该 config 的事项全部省略
- **禁止**修改 Bot 下发的 SQL 文本（禁止追加 WHERE、禁止改列名、禁止 DML/DDL、禁止分号多语句）
- **禁止**复用历史轮次缓存；每次任务必须重新执行全部 SQL

### 1. 读取 SOURCE（oabp 事项表）— meeting-mysql__mysql_query

Bot 已从 preset 取出**已填写**的 `oabpTaskSql` 写入 **[source_oabp_sql]**；未填 SQL 的 config **不会出现在数据块中**，无需处理，**任务照常进行**。

若 **[source_oabp_sql]** 为 `(none — …)` 或无任何 `config=` 组：**跳过本节**，直接仅用纪要（§2）生成 items。

对 **[source_oabp_sql]** 每一组 `config` + `schema` + `sql`（组间以 `---` 分隔）：

1. 调用 **`meeting-mysql__mysql_query`**，**原样**执行 `sql=` 后的 SELECT。
2. 将每行映射为事项字段（按列别名/列名，常见 jq_todos SQL 映射如下）：

| oabp 列（示例） | 映射到内存字段 |
|-----------------|----------------|
| `待办事项` / `task_name` | 事项名称 |
| `执行人` / `assignee_name` / nickname | 责任人 |
| `状态` / `status_label` | 状态 |
| `截止日期` / `planned_end_date` | 截止/计划时间 |
| `开始日期` / `start_date` | 开始时间（辅助） |
| `进度` / `progress` | 进度（辅助） |

3. 为每条记录保留 `sourceConfigName = config` 值（如 `preset1-comp-agenda-02`）。
4. 查询失败（超时、Access denied、语法错误）：**跳过该 config 全部事项**，在 JSON 块外的简短说明中记录 `config` + 错误摘要。

**不要**再读取 [source_feishu_urls]（已废弃；若数据块中仍出现，忽略）。

### 2. 读取会议纪要 — meeting-mysql__mysql_query

1. 调用 **`meeting-mysql__mysql_query`**，SQL 见 **[minute_query_sql]**（库 `intelligence`，勿连 localhost）。
2. 对每行：`meeting_id`、`title`、`content_markdown`（空则尝试 `content_url`）。
3. 仅使用 `generation_status='READY'` 的纪要。
4. 从纪要正文中抽取：事项名称、责任人、时间节点、状态描述、完成/截止语义。

### 3. 读取上期通报（可选）— meeting-mysql__mysql_query

若数据块含 **[output_reference_sql]**：

1. 执行该 SELECT，读取上一期已入库事项（`matter_name` / `assignee` / `time_node` / `status_label` / `category`）。
2. 用于对比「本周相对上期的变化」；**不得**直接复制上期 items 作为本期输出。
3. 若无此段或查询无行：视为无上期参考，不影响任务成功。

**不要**再读取 [output_reference_url] 飞书 Doc（已废弃；若仍出现，忽略）。

### 4. 数据处理（判定待办状态）

在内存中完成，**不要把中间分析写入最终 JSON**：

**范围（硬性）**：最终 `items` **只包含近 7 天会议纪要中提及的事项**（含同义/简称/关键词匹配）。SOURCE oabp 全表仅用于补全这些事项的**当前状态**；纪要未提及的 SOURCE 行**不得**写入 items。

1. 以 **[minute_query_sql]** 结果为主，抽取纪要中所有待办事项及责任人、时间节点。
2. 若有 **[source_oabp_sql]** 查询结果：按「事项名称 + 责任人」或关键词归并，补全/校正状态与截止日；若无 oabp 数据则仅依据纪要判定状态。
3. 每条事项**仅归入一个** `category`（互斥，禁止重复出现在多个分组）。
4. **状态 → category 映射（硬性）**：

| category | statusLabel | 判定 |
|----------|-------------|------|
| `DELAYED` | `延期` | 状态含「延期/已延期/逾期/超期/过期」；或状态非终态且有截止日且截止日 **早于当日** |
| `COMPLETED` | `已完成` | 状态为「已完成/已关闭/已办结」等终态；或纪要明确已办结 |
| `IN_PROGRESS` | `进行中` | 其余非终态（含「未开始/进行中」且未满足延期条件） |

5. **禁止错放**：`已完成` 不得入 `DELAYED`/`IN_PROGRESS`；未延期的 `进行中` 不得入 `DELAYED`。
6. SOURCE 与纪要冲突时，**以 SOURCE oabp 的 `状态` 列为准**；仍无法判断则 `IN_PROGRESS`，`statusLabel=进行中`。
7. **输出前自检**：逐条核对 `statusLabel` 与 `category` 一致；不一致则重新归类后再输出 JSON。

### 5. 输出协议（必须遵守）

**不写飞书 Doc、不写 Markdown 通报正文**。最终回复**只输出一个 JSON 块**（Bot 解析后批量 INSERT `int_weekly_matter_comparison_run` + `int_weekly_matter_comparison_item`）：

```
-----BEGIN_WEEKLY_COMPARISON_ITEMS-----
{
  "generatedAt": "YYYY-MM-DD HH:mm:ss",
  "items": [
    {
      "category": "DELAYED",
      "matterName": "事项内容",
      "assignee": "责任人姓名",
      "timeNode": "2026-06-20（截止）",
      "statusLabel": "延期",
      "sortOrder": 1,
      "sourceConfigName": "preset1-comp-agenda-02"
    }
  ]
}
-----END_WEEKLY_COMPARISON_ITEMS-----
```

**字段约束**：

- `generatedAt`：本次生成时间，格式 `YYYY-MM-DD HH:mm:ss`
- `category`：枚举 `DELAYED` | `COMPLETED` | `IN_PROGRESS`，须与 `statusLabel` 一致
- `statusLabel`：仅允许 `延期` / `已完成` / `进行中`
- `matterName`：必填
- `assignee` / `timeNode` / `sourceConfigName`：有值才输出；**缺失则省略字段**（禁止写「未提及」字符串）
- `sourceConfigName`：填该事项归属的 SOURCE config（来自 [source_config_names] / [source_oabp_sql] 的 `config=`）
- `sortOrder`：组内从 1 递增
- 某组无事项：**不要**插入占位行；该 `category` 在 `items` 中零条

**组内排序（写入 sortOrder 时遵守）**：

- `DELAYED`：逾期越重越靠前（或截止日越早越靠前）
- `COMPLETED`：完成时间降序；无完成时间用截止/计划时间降序
- `IN_PROGRESS`：有截止日的按截止日升序；无截止日排在该组末尾

### 6. 回复 Bot（必填）

JSON 块外可有一两句执行摘要（如哪些 config SQL 失败）；**最后一行必须是**：

```
-----END_WEEKLY_COMPARISON_ITEMS-----
```

**禁止**输出：`generatedReportUrl=`、飞书 Doc URL、Markdown `## 延期事项` 通报正文、第四部分附录、数据来源说明。

Bot 入库成功后，主持页通过 `host_agenda.generatedReportRunId` 或 oabpTaskSql 展示通报，**无需** Agent 写回 MySQL 或飞书。
