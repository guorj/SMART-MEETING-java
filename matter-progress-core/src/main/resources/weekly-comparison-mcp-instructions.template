## 会前事项对比 — 执行手册（oabp SOURCE + items JSON 输出）

### 0. 使用哪个 Skill、哪些 MCP（必须遵守）

| 类型 | 名称 | 用途 |
|------|------|------|
| **Skill** | `matter-progress` | 本任务专用（已由 Bot 以 `/skill:matter-progress` 触发） |
| **MCP 读 SOURCE（oabp）** | `meeting-mysql__mysql_query` | 执行 [source_oabp_sql] 中每条 SELECT（只读） |
| **MCP 读纪要** | `meeting-mysql__mysql_query` | 执行 [minute_query_sql] 读 `content_markdown`（库 `intelligence`） |

- **不再调用 lark-mcp 写飞书 Doc**；产物由 Bot 入库。
- oabp 默认 schema：**{{oabpSchema}}**（同实例可用 `schema.table` 限定）
- **禁止**臆造数据；读不到的 SQL 在 items 中省略该 config 的事项。
- **禁止**修改 Bot 下发的 SQL 文本；原样执行。

### 1. 读取 SOURCE（oabp 事项表）— 用 meeting-mysql__mysql_query

对数据块 **[source_oabp_sql]** 每一组 `config` + `schema` + `sql`：

1. 调用 **`meeting-mysql__mysql_query`**，**原样**执行 `sql=` 后的 SELECT（禁止改写、禁止追加 DML）。
2. 将每条记录整理为：事项名称、负责人、状态、计划/完成时间等（按实际列名映射）。
3. 查询失败时在 items 中省略该 config 的事项，并在最终回复正文简述原因。

### 2. 读取会议纪要正文 — 用 meeting-mysql__mysql_query

1. 调用 **`meeting-mysql__mysql_query`**，SQL 见数据块 **[minute_query_sql]**（库 `intelligence`）。
2. 对每行：`meeting_id`、`title`、`content_markdown`（空则尝试 `content_url`）。
3. 仅使用 `generation_status='READY'` 的纪要。

### 3. 数据处理（用于判定待办状态）

在内存中完成处理，不要把中间分析写入最终回复：

前提：最终生成报告数据记录与会议纪要中一致，只统计纪要中提及事项的完成状况
1. 从 SOURCE 与会议纪要中抽取所有事项、责任人、时间节点（完成时间、截止时间/计划时间须分开识别）。
2. 以会议纪要为主，对同名/同责任人/同关键词事项做归并，**每条事项仅归入一个分组**，会议纪要中未提及的事项不计入。
3. **状态 → 分组映射（硬性）**：
   - **延期事项**（`DELAYED`）：状态含「延期/逾期/超期/过期」；或状态非终态且有计划截止日且截止日早于当日。
   - **已完成事项**（`COMPLETED`）：状态为「已完成/已关闭/已办结」等终态。
   - **进行中事项**（`IN_PROGRESS`）：其余非终态事项。
4. **禁止错放**：状态为「已完成」不得入延期/进行中；状态为「进行中」且未延期不得入延期。
5. 若 SOURCE 与纪要冲突，以 SOURCE oabp 表状态字段为准。
6. **输出前自检**：逐条核对「状态」与所在分组一致。

### 4. 输出协议（必须遵守）

**不写飞书 Doc**。最终回复正文只输出一个 JSON 块（Bot 解析后批量入库）：

```
-----BEGIN_WEEKLY_COMPARISON_ITEMS-----
{
  "generatedAt": "YYYY-MM-DD HH:mm:ss",
  "items": [
    {
      "category": "DELAYED",
      "matterName": "事项内容",
      "assignee": "责任人姓名",
      "timeNode": "2026-06-20",
      "statusLabel": "延期",
      "sortOrder": 1,
      "sourceConfigName": "preset1-comp-agenda-02"
    }
  ]
}
-----END_WEEKLY_COMPARISON_ITEMS-----
```

字段约束：

- `category`：枚举 `DELAYED` | `COMPLETED` | `IN_PROGRESS`，须与 `statusLabel` 一致
- `statusLabel`：仅允许 `延期` / `已完成` / `进行中`
- `matterName`：必填
- `assignee` / `timeNode` / `sourceConfigName`：缺失时**省略该字段**（不要写「未提及」字符串）
- `sourceConfigName`：单 SQL 时填该 SQL 所属 config；多 SOURCE 共享 SQL 时按事项归属填，无法判断省略
- 某组无事项：`items` 数组中不出现该 category 的条目（不插占位行）
- 组内排序：
  - `DELAYED`：按逾期程度从重到轻（sortOrder 递增）
  - `COMPLETED`：按完成时间降序
  - `IN_PROGRESS`：按截止日升序

**禁止**在 JSON 块外输出 Markdown 通报正文、飞书 Doc URL、`generatedReportUrl=` 等旧格式。
